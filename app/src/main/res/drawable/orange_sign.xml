<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <solid android:color="@color/orange" />

    <!-- just to max it out -->
    <corners android:radius="@dimen/image_size"/>
    <!--Maybe it doesn't make sense, but i made use of existing dependencies-->
    <padding
        android:bottom="@dimen/image_padding"
        android:top="@dimen/image_padding"
        android:left="@dimen/text_size"
        android:right="@dimen/text_size" />

</shape>